#include <stdio.h>
#include <math.h>
int main(void){
int a;
scanf("%d",&a);
printf("The Postive number is %d",a);
int sum = pow(a,1)+ pow(a,2) + pow (a,3);
printf("sum of first three powers is %d", sum);

}
